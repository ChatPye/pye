# GitHub Actions

| Workflow | Purpose |
|----------|---------|
| `ci.yml` | Type-check + build on PR and push to `main` |
| `deploy-vercel.yml` | Deploy to Vercel on push to `main` |

See [docs/DEPLOY.md](../docs/DEPLOY.md).

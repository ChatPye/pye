{
  "name": "@chatpye/web",
  "version": "1.0.0",
  "private": true,
  "description": "ChatPye Workspace — Next.js web application (currently hosted at repository root during monorepo migration)",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "type-check": "tsc --noEmit -p ../../tsconfig.json"
  }
}
